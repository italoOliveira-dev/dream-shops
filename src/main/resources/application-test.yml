spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: 'sa'
    password:
  h2:
    console:
      path: '/h2-console'
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true